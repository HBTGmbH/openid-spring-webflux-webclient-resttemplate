server:
  port: 8114
spring:
  security:
    oauth2:
      client:
        provider:
          massnahmen:
            issuer-uri: http://localhost:8000/auth/realms/ems
        registration:
          massnahmen:
            client-id: admin-cli
            authorization-grant-type: password
